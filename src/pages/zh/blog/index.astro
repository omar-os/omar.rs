---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("zh-blog")).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime(),
);
---

<BaseLayout title="博客" description="OMAR 项目的更新和文章" lang="zh-CN">
  <div class="blog-index container">
    <h1>博客</h1>
    <p class="blog-desc">OMAR 项目的更新、发布和技术文章。</p>
    <div class="posts">
      {
        posts.map((post) => (
          <a href={`/zh/blog/${post.id}/`} class="post-card">
            <time>
              {new Date(post.data.date).toLocaleDateString("zh-CN", {
                year: "numeric",
                month: "short",
                day: "numeric",
              })}
            </time>
            <h2>{post.data.title}</h2>
            {post.data.description && <p>{post.data.description}</p>}
          </a>
        ))
      }
    </div>
  </div>
</BaseLayout>

<style>
  .blog-index {
    max-width: 48rem;
    padding-top: 3rem;
    padding-bottom: 4rem;
  }

  .blog-index h1 {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  .blog-desc {
    color: var(--text-dim);
    margin-bottom: 2.5rem;
  }

  .posts {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .post-card {
    display: block;
    padding: 1.5rem;
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    text-decoration: none;
    transition:
      border-color 0.2s,
      transform 0.2s;
  }

  .post-card:hover {
    border-color: var(--accent);
    transform: translateY(-1px);
    box-shadow: 0 2px 20px rgba(201, 168, 76, 0.08);
  }

  .post-card time {
    font-size: 0.8rem;
    color: var(--text-dim);
    font-family: var(--font-mono);
  }

  .post-card h2 {
    font-size: 1.25rem;
    color: var(--accent);
    margin: 0.25rem 0 0.5rem;
  }

  .post-card p {
    font-size: 0.9rem;
    color: var(--text-dim);
    line-height: 1.5;
  }
</style>
