---
import DocsLayout from "../../../layouts/DocsLayout.astro";
---

<DocsLayout
  title="快速开始"
  description="安装 OMAR 并启动你的第一个多智能体工作流。"
  lang="zh-CN"
>
  <p class="docs-lead">
    OMAR 是面向智能体系统的确定性编排运行时。它基于
    tmux，让你在一个终端内指挥深层并行 AI 智能体团队。
  </p>

  <h2>前置依赖</h2>
  <ul>
    <li>
      <strong>tmux 3.0+</strong> — macOS 运行 <code>brew install tmux</code
      >，Debian/Ubuntu 运行
      <code>apt install tmux</code>。
    </li>
    <li>
      至少安装一个受支持的编程智能体：Claude Code、Codex CLI、Cursor
      CLI、Opencode 或 Google Antigravity CLI。
    </li>
  </ul>

  <h2>安装</h2>
  <h3>一键安装（推荐）</h3>
  <pre><code>curl -fsSL https://omar.rs/install.sh | sh</code></pre>
  <p>
    安装脚本支持 macOS 和 Linux，默认将 OMAR 二进制安装到 <code
      >/usr/local/bin</code
    >。
  </p>

  <h3>Homebrew</h3>
  <pre><code>brew install omar-os/omar/omar</code></pre>
  <p>OMAR 的 Homebrew tap 会自动安装 tmux 依赖。</p>

  <h3>从源码编译</h3>
  <pre><code>git clone https://github.com/omar-os/omar.git
cd omar
make install</code></pre>
  <p>从源码编译需要 Rust 1.70+ 和 GNU Make。</p>

  <h2>启动 OMAR</h2>
  <pre><code>omar</code></pre>
  <p>OMAR 会自动检测已安装的后端。也可使用 <code>-a</code> 明确选择：</p>
  <table>
    <thead>
      <tr><th>后端</th><th>命令</th></tr>
    </thead>
    <tbody>
      <tr><td>Claude Code</td><td><code>omar -a claude</code></td></tr>
      <tr><td>Codex CLI</td><td><code>omar -a codex</code></td></tr>
      <tr><td>Cursor CLI</td><td><code>omar -a cursor</code></td></tr>
      <tr><td>Opencode</td><td><code>omar -a opencode</code></td></tr>
      <tr><td>Google Antigravity CLI</td><td><code>omar -a agy</code></td></tr>
    </tbody>
  </table>

  <h2>运行第一个工作流</h2>
  <ol>
    <li>运行 <code>omar</code>，打开仪表盘和执行助理。</li>
    <li>
      将以下任务粘贴到执行助理窗口：
      <pre><code>Run https://github.com/omar-os/omar/blob/main/prompts/tests/project-factory.md</code></pre>
    </li>
    <li>
      观察智能体团队自动组建。使用方向键切换智能体，按 <code>Tab</code> 进入下层，按
      <code>Shift+Tab</code> 返回上层。
    </li>
    <li>
      完成后告诉执行助理：
      <pre><code>Shutdown the test project and its agents.</code></pre>
    </li>
  </ol>

  <p>
    可在 <a href="https://github.com/omar-os/omar">GitHub</a> 查看源码、版本发布和问题追踪。
  </p>
</DocsLayout>

<style>
  .docs-lead {
    color: var(--text-bright);
    font-size: 1.1rem;
    line-height: 1.85;
  }
</style>
